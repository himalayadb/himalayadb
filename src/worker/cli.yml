name: worker
version: "1.0"
author: HimalayaDB
about: Worker Node
args:
  - verbose:
      short: v
      multiple: true
      about: Sets the level of verbosity
subcommands:
  - start:
      about: Start the worker node
      version: "1.0"
